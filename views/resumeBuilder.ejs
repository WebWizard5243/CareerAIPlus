<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>CareerAIPlus</title>
  <style>
    /* Global Styles */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    body {
      background-color: #212121;
      color: #fff;
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 20px;
    }

    /* Header/Navigation */
    header {
      background-color: #a9a9a9;
      padding: 20px 25px;
    }

    .logo {
      font-size: 24px;
      font-weight: bold;
      display: flex;
      align-items: center;
    }

    .logo svg {
      width: 25px;
      height: 25px;
      margin-right: 7px;
    }

    nav {
      color: black;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    nav ul {
      list-style: none;
      margin: 0;
      padding: 0;
      display: flex;
    }

    nav ul li {
      margin: 0 15px;
    }

    nav ul li a {
      text-decoration: none;
      color: black;
      transition: all 0.3s;
    }

    nav ul li a:hover {
      color: rgb(239, 239, 239);
    }

    nav ul li a.active {
      color: white;
      /* background-color: white; */
      font-weight: bolder;
      /* border: 1px solid white; */
      /* padding: 8px;
          border-radius: 25px; */
    }

    .hamburger {
            display: none;
            cursor: pointer;
            flex-direction: column;
            justify-content: space-around;
            width: 30px;
            height: 25px;
            background: none;
            border: none;
            z-index: 1001;
        }

        .hamburger-line {
            width: 100%;
            height: 3px;
            background-color: black;
            transition: all 0.3s ease;
        }
    /* Main Content */
    .main-content {
      padding: 30px 0;
      display: grid;
      grid-template-columns: 1fr;
      gap: 20px;
    }

    /* Cards */
    .card {
      background-color: #2a2a2a;
      border-radius: 8px;
      padding: 25px;
      margin-bottom: 20px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    .card-title {
      font-size: 1.25rem;
      margin-bottom: 20px;
      font-weight: 600;
    }

    .card p {
      font-weight: 400;
    }

    /* Resume Upload */
    .upload-area {
      margin-top: 15px;
      color: #a0a0a0;
    }
    
    /* AI Feedback */
    .feedback-list {
      list-style: none;
      margin-top: 15px;
    }
    #submit {
      float: right;
      padding: 10px 20px;
    border: none;
    cursor: pointer;
    border-radius: 25px;
    background-color: red;
    color: white;
    transition: all 0.3s;
    }
    .feedback-list li {
      margin-bottom: 10px;
      line-height: 1.2;
      font-size: 0.95rem;
      font-weight: 100;
    }

    /* Resume Templates */
    .templates-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 15px;
      margin-top: 20px;
      margin-bottom: 110px;
    }

    .template-card {
      background-color: #fff;
      border-radius: 24px;
      overflow: hidden;
      transition: transform 0.3s;
    }

    .template-card:hover {
      transform: translateY(-5px);
    }

    .template-image {
      width: 160px;
      height: 160px;
      display: block;
      border-radius: 25px;
      margin: 10px;
    }

    .template-btn {
      display: block;
      text-align: left;
      padding: 12px;
      background-color: #fff;
      color: #333;
      text-decoration: none;
      font-weight: 500;
      margin-left: 10px;
      margin-bottom: 10px;
      border-radius: 25px;
      width: 15vw;
    }
    .template-btn:hover{
      background-color: #bebbbb;
    }
    .file-title {
      color: black;
      margin-left: 15px;
    }
    /* LinkedIn Integration */
    .connect-btn {
      display: inline-block;
      background-color: #e60023;
      color: white;
      padding: 10px 20px;
      border-radius: 25px;
      text-decoration: none;
      font-weight: 500;
      margin-top: 15px;
      transition: background-color 0.3s;
    }

    .connect-btn:hover {
      background-color: #d1001f;
    }

    /* Footer */
    footer {
      background-color: #a9a9a9;
      text-align: left;
      padding: 20px;
      margin-top: 10px;
      color: black;
    }

    .footer-content {
      display: flex;
      justify-content: space-evenly;
    }

    footer .footer-links a {
      margin-bottom: 8px;
      color: black;
      text-decoration: none;
    }

    footer .footer-links a:hover {
      color: rgb(239, 239, 239);
      font-weight: bold;
    }

    .footer-links h3 {
      font-weight: bold;
      font-size: 1.15rem;
    }

    .footer-links p {
      font-size: 1.05rem;
    }

    .footer-links {
      display: flex;
      flex-direction: column;
      gap: 10px;
    }

    /* Media Queries */
    @media (max-width: 768px) {
      .main-content {
        grid-template-columns: 1fr;
        padding: 25px;
      }

      .templates-grid {
        grid-template-columns: repeat(2, 1fr);
      }
            nav ul {
                display: none;
                position: fixed;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                background-color: #a9a9a9;
                flex-direction: column;
                justify-content: center;
                align-items: center;
                z-index: 100;
            }

            nav ul li {
                margin: 15px 0;
                text-align: center;
            }

            .hamburger {
                display: flex;
                position: relative;
                z-index: 1001;
            }

            /* Hamburger menu animation */
            .hamburger.active .hamburger-line:nth-child(1) {
                transform: rotate(45deg) translate(5px, 5px);
            }

            .hamburger.active .hamburger-line:nth-child(2) {
                opacity: 0;
            }

            .hamburger.active .hamburger-line:nth-child(3) {
                transform: rotate(-45deg) translate(5px, -5px);
            }

            nav ul.show {
                display: flex;
            }
            .footer-content{
  display: flex;
  justify-content: space-between;
}
.footer-links h3{
  font-size: 0.90rem;
}
.footer-links a {
  font-size: 0.75rem;
}
.footer-links p {
  font-size: 0.80rem;
}
        }
    @media (max-width: 480px) {
      .templates-grid {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>

<body>
  <!-- Header/Navigation -->
  <%- include('partials/header', {title: ' resume Review ', page: 'assessment'}) %>
  <!-- Main Content -->
  <main class="container main-content">
    <div class="left-column">
      <!-- Resume Upload -->
      <section class="card">
        <form action="/ResumeReview" method="post" enctype="multipart/form-data">
        <h2 class="card-title">Upload Your Résumé</h2>
        <input type="file" class="choose-file" name="resume" >
        <input type="submit" id="submit">
      </form>
        
      </section>

      <!-- AI Feedback -->
      <section class="card">
        <h2 class="card-title">AI-Driven Feedback</h2>
        <% if(locals.review){ %>
        <p>Your résumé has been analyzed. Here are some suggestions:</p>
        <ul class="feedback-list">
          <li><%= review.summary %></li>
          <li><%= review.formattingFeedback %></li>
          <li><%= review.clarityFeedback %></li>
          <li> <%= review.grammarFeedback %></li>
          <li> Rating : <%= review.rating %></li>
        </ul>
        <% } else { %>
          <p> Your Review will be shown here</p>
          <% } %>
      </section>

      <!-- Resume Templates -->
      <section class="card">
        <h2 class="card-title">Auto-Generated Résumé Templates</h2>
        <div class="templates-grid">
          <% if(locals.files){ %>
          <% files.forEach((file, index) => { %>
            <div class="template-card">
              <img src="/img/resumeDemo/resume<%= index + 1 %>.jpeg" alt="Resume Template <%= index + 1 %>" class="template-image" />
              <h3 class="file-title"><%= file.title %></h3>
              <a href=<%= file.downloadUrl %> class="template-btn">download Template</a>
            </div>
          <% }) %>
          <% } else { %>
            <p> Sample Resume will be generated for you to download based on your uploaded resume.</p>
            <% } %>
        </div>
      </section>
    </div>

    <!-- <div class="right-column">
      LinkedIn Integration -->
      <!-- <section class="card">
        <h2 class="card-title">LinkedIn Integration</h2>
        <p>
          Connect your LinkedIn account to enhance your résumé with your
          professional network and recommendations.
        </p>
        <a href="#" class="connect-btn">Connect LinkedIn</a>
      </section>
    </div> --> 
  </main>

  <!-- Footer -->
  <%- include('partials/footer') %>
  <script src="/js/index.js"></script>
</body>

</html>